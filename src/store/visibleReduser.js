// import { VISIBLE_BREAD } from "./types";

// const visibleBread = {
//   display: "none",
// };

// export const visibleReduser = (state = visibleBread, action) => {
//   // if (action.type == "VISIBLE_BREAD") {
//   //   return console.log(state);
//   // }
//   console.log(action);

//   switch (action.type) {
//     case VISIBLE_BREAD:
//       return { ...state, visibleBread };
//     default:
//       return state;
//   }
// };
